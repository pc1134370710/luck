spring:

  cloud:
    nacos:
      discovery:
        enabled: true
        # nacos 注册地址
        server-addr: http://127.0.0.1:8848
        username: nacos
        password: nacos
      config:
        enabled: false
  application:
    name: gateway

#    context-path: /gz